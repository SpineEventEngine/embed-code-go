# Missing Pattern

This scenario fails because the source file exists, but no line matches the
requested line pattern.

<embed-code file="$java/org/showcase/Greeting.java" line="doesNotExistPattern"></embed-code>
```java
```
