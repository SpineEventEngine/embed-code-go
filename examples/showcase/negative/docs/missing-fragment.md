# Missing Fragment

This scenario fails because the source file exists, but the requested named
fragment does not.

<embed-code file="$java/org/showcase/Greeting.java" fragment="does not exist"></embed-code>
```java
```
