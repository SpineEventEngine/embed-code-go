# Missing Source

This scenario fails because the instruction points to a file that cannot be
resolved from the configured source roots.

<embed-code file="$java/org/showcase/DoesNotExist.java"></embed-code>
```java
```
