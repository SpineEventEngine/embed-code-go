# Missing Code Fence

This scenario fails because every active instruction must be followed by a
Markdown code fence.

<embed-code file="$java/org/showcase/Greeting.java" fragment="main()"></embed-code>
This line is not a code fence.
