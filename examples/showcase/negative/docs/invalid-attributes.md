# Invalid Attributes

This scenario fails because `fragment` cannot be combined with `line`, `start`,
or `end`.

<embed-code file="$java/org/showcase/Greeting.java" fragment="main()" line="main"></embed-code>
```java
```
