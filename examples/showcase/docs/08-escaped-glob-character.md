# Escaped Glob Characters

Backslashes match glob control characters literally. This case embeds a line
that contains a literal asterisk.

<embed-code file="$text/glob-patterns.txt" line="Use \* to multiply"></embed-code>
```text
Use * to multiply
```
