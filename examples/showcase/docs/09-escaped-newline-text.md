# Escaped Newline Text

Use `\\n` when the source line contains the characters backslash and `n`.
Quote characters may be escaped as `\"` inside instruction attributes.

<embed-code
  file="$java/org/showcase/PatternSamples.java"
  line="ESCAPED_NEWLINE = \"\\n\""></embed-code>
```java
private static final String ESCAPED_NEWLINE = "\n";
```
